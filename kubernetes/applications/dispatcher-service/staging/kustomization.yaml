apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - github.com/JoshLessard/dispatcher-service/k8s?ref=main

patches:
  - path: patch-env.yaml

configMapGenerator:
  - behavior: merge
    files:
      - application-staging.yaml
    name: dispatcher-config

images:
  - name: dispatcher-service
    newName: ghcr.io/joshlessard/dispatcher-service
    newTag: latest

replicas:
  - name: dispatcher-service
    count: 2