apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - github.com/JoshLessard/order-service/k8s?ref=main

patches:
  - path: patch-env.yaml

configMapGenerator:
  - behavior: merge
    files:
      - application-staging.yaml
    name: order-config

images:
  - name: order-service
    newName: ghcr.io/joshlessard/order-service
    newTag: latest

replicas:
  - name: order-service
    count: 2
