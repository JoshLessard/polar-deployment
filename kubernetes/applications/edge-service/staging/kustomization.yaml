apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - github.com/JoshLessard/edge-service/k8s?ref=main

patches:
  - path: patch-env.yaml

configMapGenerator:
  - behavior: merge
    files:
      - application-staging.yaml
    name: edge-config

images:
  - name: edge-service
    newName: ghcr.io/joshlessard/edge-service
    newTag: latest

replicas:
  - name: edge-service
    count: 2
